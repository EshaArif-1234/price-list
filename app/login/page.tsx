import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Admin login — Price List",
  description: "Internal administrator sign-in for the catalog dashboard.",
};

export { default } from "../client/login/page";
